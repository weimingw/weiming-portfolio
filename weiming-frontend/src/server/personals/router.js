/**
 * This will be used if I ever need to make calls for my personal pages
 * Very unlikely but who knows?
 */
import express from 'express';

export function createPersonalsRouter() {
    const router = express.Router();
    return router;
}

function setupRouter(router) {

}
